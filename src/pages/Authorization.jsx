import React from 'react';
import './Authorization.page.css'

const Authorization = () => {
    return (
        <div>
            Authorization
        </div>
    );
};

export default Authorization;